diff --git "a/D:\\workspace\\dsh-local-patches-backup\\fileuploads-pasteinput-20260820-131146\\client.js" "b/D:\\workspace\\dsh-file-uploads\\client.js"
index 424284c..b182962 100644
--- "a/D:\\workspace\\dsh-local-patches-backup\\fileuploads-pasteinput-20260820-131146\\client.js"	
+++ "b/D:\\workspace\\dsh-file-uploads\\client.js"	
@@ -165,11 +165,11 @@ window.__ModuleLoader__.load({
 
       attach(sessionId, file) {
         const key = String(sessionId)
-        this.clearInFlight(key)
         const entry = { ...file, ref: `${key}-${++this.counter}` }
         if (!this.insertReference(sessionId, entry)) {
           throw new Error('输入框正忙，请稍后重新选择该文件')
         }
+        this.clearInFlight(key)
         const next = [...this.pendingFor(key), entry]
         this.pending.set(key, next)
         this.refIndex.set(entry.ref, { sessionId: key, entry })
